Función para calcular el área y la circunferencia del círculo
def calcular_area_y_circunferencia():
    radio = float(input("Ingresa el radio del círculo: "))
    
    # Calcular el área y la circunferencia
    area = math.pi * radio**2
    circunferencia = 2 * math.pi * radio
    
    # Mostrar los resultados
    print(f"El área del círculo es: {area:.2f} unidades cuadradas.")
    print(f"La circunferencia del círculo es: {circunferencia:.2f} unidades.")

# Llamar a la función para realizar los cálculos
calcular_area_y_circunferencia()
